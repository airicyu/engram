# Copy to .env (gitignored). Relative paths resolve from the repo root.
# Default when unset: ./demo-engram-lite-data and port 8797.
ENGRAM_LITE_STORE_DIR=./demo-engram-lite-data
ENGRAM_LITE_PORT=8797
