# Recall 注入近程未來視（backlog）

← [backlog](./INDEX.md) · 未來視雙區（已出貨）：[0.17.0](../0.17.0/INDEX.md)

> **狀態：** 刻意延後。0.4.0／**0.17.0** 皆 **不**把未來視寫進 search／ask packet。

## 決定緣由

先把未來視（寫入、人審、過期痕跡 + 硬抹）做起來；0.17 再把存法改成 `hot.md`／`later.md` 並穩定入夢前維護。是否／如何進 Seek 等用過再定，避免與寫入契約纏在一起。

## 日後可討論

- 限量未過期塊 vs 僅 `q` 指向未來時才注入
- **建議預設只注入 `hot`**（`later` 僅瀏覽／入夢對照）；對齊 0.17 熱區語意
- 勿另開 mindzone／第二套注入源（mindzone 語意已併入 0.17 `hot`）
