# Copy to `.env` in this directory (`web/`) and adjust.
# Bun loads `.env` automatically when you `bun run` from here.

# UI listen port
# WEB_PORT=8788

# Upstream Engram API (proxied at /api/*)
# ENGRAM_URL=http://localhost:8787
