{
  "name": "engram-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "reset": "bun run src/cli/reset.ts",
    "test:phases": "bun run src/cli/self-test.ts"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "~7.0.2"
  }
}
